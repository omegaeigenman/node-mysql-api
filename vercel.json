{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["swagger.yaml"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.ts" }
  ]
}
